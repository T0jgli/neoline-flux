apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kalandozas-ingress
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: |
      kube-system-security-headers@kubernetescrd
spec:
  tls:
    - hosts:
        - kalandozas.hu
      secretName: kalandozas-tls
  rules:
    - host: kalandozas.hu
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kalandozas-service
                port:
                  number: 80
