---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: kalandozas-tls
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: bitwarden-secretsmanager
    kind: ClusterSecretStore
  target:
    name: kalandozas-tls
    template:
      type: kubernetes.io/tls
  dataFrom:
    - extract:
        key: kalandozas-kalandozas-tls
        decodingStrategy: Base64
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: kalandozas-env
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: bitwarden-secretsmanager
    kind: ClusterSecretStore
  target:
    name: kalandozas-env
  dataFrom:
    - extract:
        key: kalandozas-kalandozas-env
