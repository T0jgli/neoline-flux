apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository
metadata:
  name: contibus
spec:
  image: registry.kvlk.hu/contibus
  interval: 1m
  secretRef:
    name: regcred
---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: contibus-policy
spec:
  imageRepositoryRef:
    name: contibus
  filterTags:
    pattern: "^(?P<run>[0-9]+)-[a-f0-9]+$"
    extract: "$run"
  policy:
    numerical:
      order: asc
