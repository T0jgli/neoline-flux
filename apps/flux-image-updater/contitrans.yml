apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository
metadata:
  name: contitrans
spec:
  image: registry.kvlk.hu/contitrans
  interval: 1m
  secretRef:
    name: regcred
---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: contitrans-policy
spec:
  imageRepositoryRef:
    name: contitrans
  filterTags:
    pattern: "^(?P<run>[0-9]+)-[a-f0-9]+$"
    extract: "$run"
  policy:
    numerical:
      order: asc
